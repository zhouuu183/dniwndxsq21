# V5 耳环找回专项交接文档

更新时间：2026-08-17  
适用分支：`v5-earring-instance-redesign-20260809`

## 1. 项目中 V5 的位置

HairFastGAN 先完成发型和发色迁移，V5 是最后的 PP（post-process）阶段。V5 的耳环分支只负责从源图恢复源人物真实存在的耳饰，不应改变已经完成的迁移发型、发色或脸部主体。

```text
源图 + 参考发型/发色
        -> HairFast 发型/发色迁移结果 target
        -> V5：判断目标耳部是否可见
        -> V5：从源图提取真实耳饰实例
        -> V5：只把耳饰实体写回 target
        -> final
```

本专项只讨论 V5 耳环问题。不要修改 V8 发色流程、不要为了耳环问题重新设计脸部细节回传、不要新增或修改 `tests`。

## 2. 最终目标

V5 必须同时满足以下条件：

1. 源图存在普通实心耳环、耳钉或长耳环，且迁移结果露出相应耳朵或耳垂时，完整恢复真实耳饰；
2. 恢复的是源耳饰本体，不是相邻的草地、背景、源头发、耳朵阴影或随机高亮；
3. 源图没有耳饰时，绝不能在目标耳部生成黑点、小圆环、金属碎块或重复耳饰；
4. 源图是短发、耳环后面是背景，而目标发型是长发时，长耳环应能恢复，但耳环后的源背景绝不能覆盖目标头发并形成“空洞”；
5. 空心圆环只恢复环体和必要的耳垂连接部分，圆孔内部必须保留已经完成迁移的目标图背景或头发；
6. 左右耳独立处理，任何一侧的存在、可见性、形状和写回结果不能错误影响另一侧；
7. 不产生黑块、白丝、灰边、背景边框、耳朵和头发断开，且不能明显降低训练/推理速度。

## 3. 原有方案和当前问题的根源

旧代码目录 `E:\BaiduNetdiskDownload\HairFastGAN\besthairfast\hairfast_pp修改完毕` 对普通实心耳环的召回较好：很多耳环能被找回。但它的写回掩码范围过宽，耳环周围的源背景、源头发和阴影也会写回。因此当目标耳侧被迁移长发覆盖时，源背景会挖掉目标头发，造成耳下空洞、发型断裂和背景线。

当前 V5 重构尝试把“耳环实体”和“耳环周围可搜索区域”分开，以消除空洞。但目前保护条件叠加过多，普通耳环和圆环的召回显著下降。问题不是简单地提高一个阈值：

- 放宽搜索/写回会重新引入草地、头发、耳朵边缘等误检，并形成空洞；
- 收紧搜索/写回会把真实小耳钉、低对比度耳环、长耳环下半段和圆环大部分过滤掉；
- 圆环的“外圈、内圈、孔洞”是三种不同区域，不能把整块外接矩形或填充区域都从源图复制回来；
- 目标耳部可见性只应决定“这一侧是否允许恢复”，不能作为像素级裁剪，把已经确认的完整长耳环或圆环裁成短弧。

因此正确方向不是回到旧版的宽掩码，也不是继续单纯收紧保护，而是可靠取得“真实耳饰实例 alpha”。普通耳饰只写实例本体；圆环写环体，孔洞由 target 保留。

## 4. 当前相关代码与职责

| 文件 | 当前职责 |
| --- | --- |
| `models/ear_modules_v5.py` | 耳朵/耳垂 ROI、parser 耳饰标签、视觉候选、源图高分辨率耳饰实例、圆环外/内轮廓及孔洞提取、对齐工具 |
| `models/postprocess_v5.py` | 左右耳可见性 gate、耳饰搜索/写回 mask、目标头发保护、PP 合成、圆环孔洞保持 target |
| `models/Blending_v5.py` | V5 总流程中调用 PP，并输出耳饰调试信息 |
| `hair_swap_v5.py` | V5 推理入口和参数传递 |
| `scripts/pp_gen_v5.py` | 生成训练对、源图高分辨率耳饰实例/圆环监督和写回掩码 |
| `scripts/pp_train_v5.py` | 读取 PP 数据集、训练 V5 PP 网络和保存验证预览 |
| `losses/pp_losses_v5.py` | PP 和耳饰相关训练损失 |

当前数据集 schema 是 `11`。它要求先在源图原始分辨率提取普通耳饰/圆环实例，再降低到 PP 分辨率保存监督；它还固定了“目标耳/耳垂原始可见性决定侧开关”的契约。schema 10 及更早数据不能混用。

`scripts/pp_gen_v5.py` 中此前的 `aligned_masks` 未定义错误已修复：`earring_core_mask`、`earring_completion_mask`、`earring_object_mask`、`earring_filled_mask` 现在从 `earring_policy` 写入。此前中断的数据目录是半成品，生成数据时必须使用新的输出目录并从头生成。

本版额外固定三项策略：V5 不再注入源脸高频细节，也不再羽化目标发际线，避免脸部色环；普通耳饰最终 RGB 仅接受逐侧关联的 parser 实例，耳旁视觉纹理只能作为圆环闭环验证的候选，不能生成普通耳饰；目标耳下方的头发不参与侧可见性判定，只在目标耳朵和耳垂本体均被遮住时关闭该侧恢复。

small profile 当前使用同一个 `FFHQ_3000` 目录作为 source、shape donor 和 color donor。生成器按文件 stem 作为 FFHQ 身份键，强制每个 triplet 的三张图来自三个不同身份；不同 triplet 之间允许重复 donor。标准 FFHQ 一图一人时该约束有效；若以后一个身份有多张不同 stem 的图，需要先提供身份分组规则。

## 5. 当前已经部署的空洞保护及其作用

这些保护本身是合理的，但其组合也是当前召回下降的主要风险点，应逐项观察，而不是盲目继续增大阈值。

| 保护 | 目的 | 可能造成的副作用 |
| --- | --- | --- |
| 无耳环 hard gate | 没有 parser/强证据时关闭耳环支路，防止凭空生成耳环 | parser 漏标、小耳钉或低对比度耳环会直接漏检 |
| 左右耳独立可见性 gate | 目标该侧被头发遮住时不恢复源耳饰 | 可见性判断偏保守时，露耳垂的真实耳环也会被关闭 |
| source background/hair/semantic blocker | 不把耳环后的源背景、源发和普通皮肤写回 | 真实耳环被 parser 误标为背景/头发时会被误删 |
| object-supported write mask | 只允许有实体证据的像素写回，阻止宽 ROI 形成空洞 | 长耳环末端、细连接线和圆环细线容易断裂 |
| target-hair overlap 限制 | 防止源内容穿透目标长发 | 真实耳环与目标头发相邻或部分重叠时被过度裁剪 |
| source-native 实例提取 | 避免在 256 分辨率提前丢失细耳钉和细圆环 | 高分辨率候选/轮廓规则仍可能拒绝低对比度或部分遮挡实例 |
| 圆环内孔保持 target | 防止圆孔内的源背景/头发被恢复 | 外圈不完整或孔洞判断失败时，可能只剩短弧、孔内残留或错误填充 |
| learned fallback 默认关闭 | 阻止模型补出不存在的耳环、重复耳环 | 无法用网络补齐被实例提取漏掉的真实边缘；目前优先保证不虚构 |
| `earring_align_max_shift = 0` | 不移动源耳饰，避免二次偏移和重复耳环 | 仅在源/目标坐标确实需要校正的少数样本中可能降低贴合度 |

## 6. 尚未解决的问题

### 6.1 普通耳环、耳钉和长耳环漏恢复

- 源图有真实耳饰，迁移结果也露出耳朵或耳垂，但最终图完全没有耳饰；
- 长耳环常只恢复耳垂附近，下面的主体丢失；
- 小耳钉、低对比度耳饰、耳垂只露一部分的耳饰尤其容易漏检；
- 有时一侧能恢复，另一侧条件相似却不能恢复，说明逐侧证据、实例归属或可见性 gate 仍有错误。

需要定位每次漏恢复发生在哪一层：源图存在性判断、左右耳归属、目标侧可见性、实例提取、圆环识别、对齐，还是最终 alpha/写回被裁掉。禁止只看 final 图后盲目修改参数。

### 6.2 普通耳环错误恢复和黑色碎块

- 源图无耳饰时，草地、背景纹理、头发边缘或耳部阴影有时被当作耳环；
- 有真实耳环时，可能只恢复黑点、黑色碎块、金属噪声或重复耳环；
- 耳饰周围仍可能带回一圈源背景/源头发，导致耳朵附近黑块、边框或局部色块；
- 耳环与耳垂的连接线、挂钩等细结构容易丢失。

这说明“候选区域”仍被错误地当成“实例本体”，或者实例边缘 alpha 不是严格的实体边界。

### 6.3 空心圆环恢复失败

当前圆环问题包括：

- 只恢复短弧，甚至只有约五分之一圆环；
- 圆环被压扁、变粗、偏移，与源图圆环几何不一致；
- 圆环的耳垂连接线缺失；
- 圆环内孔仍含源背景或源头发；
- 某些样本只写回圆孔内部背景，却没有正确写回圆环；
- 小而不明显的圆环有时被当普通实心耳环，有时完全没有恢复；
- 圆环外侧有时还残留黑色源背景边缘。

圆环的正确监督和推理结果必须分别保存三张 mask：`ring_instance_mask`、`hoop_hole_mask`、`final_write_alpha`。只凭最终图无法判断故障是外环检测、孔洞检测还是合成造成的。

### 6.4 空洞、头发断开与耳部伪影

虽然当前代码有多层保护，以下现象仍需要作为失败案例保留：

- 源图短发且耳环后是背景时，恢复长耳环可能把背景写回目标长发，形成耳下空洞；
- 耳朵下方与迁移头发断开，即使源图并无耳环；
- 耳部附近出现黑块、白丝、灰边、源图背景线或局部肤色/阴影不一致。

其中“耳朵与头发断开”不能一概归因于耳环本体；必须先检查 final write alpha 是否非零。若 alpha 为零仍断开，根因在目标头发/耳部保护或 PP 合成，不应通过放宽耳环检测修复。

## 7. 后续修改的硬约束

1. 不能回到旧版“宽耳旁 ROI 直接复制源图”的方案；它虽然召回高，但会重现空洞；
2. 不能用无耳饰源图生成/补画耳环；误检优先级必须和漏检同等高；
3. 不可把圆环内孔当作耳饰主体，内孔必须保持 target；
4. 不可把目标耳 ROI 当作最终像素裁剪，完整实例一旦确认，只应按该侧开关决定保留或关闭；
5. 不修改 V8 发色逻辑、脸部细节策略或 `tests`；
6. 不把高分辨率重型检测无条件放进每次训练前向，先在数据生成阶段完成，并记录耗时；
7. 任何检测/掩码策略变化后，都必须重新生成完整数据集并从头训练 PP，不能混用旧 dataset parts、旧 schema 或旧 PP 权重；
8. 参考旧目录的普通耳饰高召回思路可以，但不能照搬其宽掩码、背景复制和脸部回传逻辑。

## 8. 推荐的排查和验收顺序

### 第一步：建立固定案例集

至少分为以下五类，每类包含左右耳和“目标耳朵可见/被长发遮住”两种条件：

1. 源图无耳饰；
2. 普通实心耳环或耳钉；
3. 长耳环，源图耳环后为背景；
4. 大空心圆环；
5. 小/低对比度空心圆环。

### 第二步：每个案例保存中间证据

对每一侧耳朵保存并对比：

```text
source image
target before PP
source parser earring mask
source native instance mask
left/right target visibility gate
ring instance mask（如有）
hoop hole mask（如有）
final earring write alpha
final image
```

应先根据这些中间结果把错误归为“漏检”“错误归属”“目标可见性误关”“实例不完整”“孔洞错误”或“合成泄漏”，再修改相应模块。

### 第三步：数据与训练验证

1. 使用 schema 11 对应的全新 PP 输出目录运行 `python scripts/pp_gen_v5.py`；
2. 确认所有 parts 和 `dataset_config.json` 完整生成；
3. 用同一目录从头训练 `python scripts/pp_train_v5.py`；
4. 在固定案例集上比较旧方案、新方案和不启用 V5 的 target；
5. 分别记录无耳饰误检率、普通耳饰召回、长耳环完整度、圆环闭合度、圆孔目标保留率和耳下空洞率。

没有完成以上数据生成、重新训练和固定案例验收前，不应宣称耳环问题已经解决。
